- set_meta_tags title: '検索結果', description: '検索画面'

= render "header"

.wrapper
  .main-content
    - @blogs.each do |blog|
      = render partial: "blog", locals: { blog: blog}

    .pagenate
      = paginate(@blogs)

  = render 'side'

= render "footer"
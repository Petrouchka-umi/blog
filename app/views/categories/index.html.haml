- set_meta_tags title: 'カテゴリ一覧', description: '親カテゴリー一覧'

= render "blogs/header"

.wrapper
  .category-contents
    - @parents.each do |parent|
      .category-parent
        = link_to "#{parent.name}", parent_category_path(parent), class: "parent"

  = render 'blogs/side'

= render "blogs/footer"